C51 COMPILER V9.60.7.0   SPEED_SCAN                                                        08/14/2026 16:22:47 PAGE 1   


C51 COMPILER V9.60.7.0, COMPILATION OF MODULE SPEED_SCAN
OBJECT MODULE PLACED IN .\Release\Objects\speed_scan.obj
COMPILER INVOKED BY: C:\Keil_v5\C51\BIN\C51.EXE ..\..\User\speed_scan.c LARGE OPTIMIZE(9,SIZE) BROWSE ORDER INTVECTOR(0X
                    -000C) INCDIR(..\..\Libraries\Include;..\..\User;..\..\User\lib;..\..\Hardware) INTERVAL(3) DEBUG OBJECTEXTEND PRINT(.\Re
                    -lease\Listings\speed_scan.lst) OBJECT(.\Release\Objects\speed_scan.obj)

line level    source

   1          #include "speed_scan.h"
   2          #include "aip3368h_display.h"
   3          #include "instrument.h" // 包含仪表的参数
   4          #include "mileage.h"    // 包含里程的参数 distance
   5          
   6          #if SPEED_SCAN_ENABLE
   7          
   8          static volatile bit flag_is_speed_scan_over_time = 0; // 速度检测是否一直没有脉冲到来，导�
             -��超时
   9          
  10          static volatile u32 speed_pulse_cnt = 0;    // 记录脉冲个数，在定时器中断累加
  11          static volatile u32 speed_scan_time_ms = 0; // 记录扫描时间，在定时器中断累加
  12          
  13          static volatile u32 cur_speed_scan_time = 0; // 当前检测时速所用时间
  14          static volatile u32 cur_speed_scan_pulse = 0; // 当前检测时速所用时间内检测到的脉冲个数
  15          
  16          // 计数器，计数满一段时间后，更新显示
  17          static volatile u8 aip3368h_display_speed_refresh_time_cnt = 0;
  18          
  19          // 滑动平局数组
  20          #define SPEED_FILTER_ARRAY_SIZE (40)
  21          // #define SPEED_FILTER_ARRAY_SIZE (20)
  22          static volatile u8 speed_filter_array[SPEED_FILTER_ARRAY_SIZE] = {0};
  23          static volatile u8 speed_filter_index = 0;
  24          
  25          void speed_filter_init(u8 speed)
  26          {
  27   1          u8 i;
  28   1          for (i = 0; i < SPEED_FILTER_ARRAY_SIZE; i++) {
  29   2              speed_filter_array[i] = speed;
  30   2          }
  31   1      
  32   1          speed_filter_index = 0;
  33   1      }
  34          
  35          void speed_filter_add(u8 speed)
  36          {
  37   1          speed_filter_array[speed_filter_index] = speed;
  38   1          speed_filter_index++;
  39   1          if (speed_filter_index >= SPEED_FILTER_ARRAY_SIZE) {
  40   2              speed_filter_index = 0;
  41   2          }
  42   1      }
  43          
  44          u8 speed_filter_get_speed(void)
  45          {
  46   1          u8 i;
  47   1          u32 sum = 0;
  48   1          u8 max_speed = 0;
  49   1          u8 min_speed = (u8)-1;
  50   1      
  51   1          // for (i = 0; i < SPEED_FILTER_ARRAY_SIZE; i++)
  52   1          // {
C51 COMPILER V9.60.7.0   SPEED_SCAN                                                        08/14/2026 16:22:47 PAGE 2   

  53   1          //     printf("speed[%u] == %u\n", (u16)i, (u16)speed_filter_array[i]);
  54   1          // }
  55   1          // printf("\n===================\n");
  56   1      
  57   1          for (i = 0; i < SPEED_FILTER_ARRAY_SIZE; i++) {
  58   2              sum += speed_filter_array[i];
  59   2      
  60   2              if (max_speed < speed_filter_array[i]) {
  61   3                  max_speed = speed_filter_array[i];
  62   3              }
  63   2      
  64   2              if (min_speed > speed_filter_array[i]) {
  65   3                  min_speed = speed_filter_array[i];
  66   3              }
  67   2          }
  68   1      
  69   1          sum -= (max_speed + min_speed);
  70   1      
  71   1          return sum / (SPEED_FILTER_ARRAY_SIZE - 2);
  72   1      }
  73          
  74          // 时速扫描的配置
  75          void speed_scan_config(void)
  76          {
  77   1          // 使用定时器扫描IO电平变化来计算脉冲
  78   1          P2_PU |= GPIO_P25_PULL_UP(0x1);    // 配置为上拉
  79   1          P2_MD1 &= ~GPIO_P25_MODE_SEL(0x3); // 输入模式
  80   1      }
  81          
  82          /**
  83           * @brief 更新检测时速的数据，由定时中断调用
  84           *
  85           */
  86          void speed_scan_update_data(void) //
  87          {
  88   1          cur_speed_scan_time += speed_scan_time_ms;
  89   1          speed_scan_time_ms = 0;
  90   1          cur_speed_scan_pulse += speed_pulse_cnt;
  91   1          speed_pulse_cnt = 0;
  92   1      }
  93          
  94          void speed_scan_timer_50us_isr(void)
  95          {
  96   1          static volatile bit last_speed_scan_level = 0; // 记录上一次检测到的引脚电平（时速检�
             -��脚）
  97   1          static volatile u16 cnt = 0;                   // 记录时速扫描的时间
  98   1          cnt++;
  99   1          if (cnt >= 20) // 每1ms进入一次
 100   1          {
 101   2              cnt = 0;
 102   2              // 更新扫描时间（如果一直没有脉冲到来给它清零，这里会一直累加）
 103   2              speed_scan_time_ms++;
 104   2      
 105   2              if (speed_scan_time_ms >= SPEED_SCAN_OVER_TIME && flag_is_speed_scan_over_time == 0) {
 106   3                  speed_scan_time_ms = 0;
 107   3                  flag_is_speed_scan_over_time = 1; // 说明超时，脉冲计数一直没有加一
 108   3              }
 109   2          }
 110   1      
 111   1          if (SPEED_SCAN_PIN) // 检测时速的引脚
 112   1          {
 113   2              if (0 == last_speed_scan_level) {
C51 COMPILER V9.60.7.0   SPEED_SCAN                                                        08/14/2026 16:22:47 PAGE 3   

 114   3                  speed_pulse_cnt++;
 115   3                  speed_scan_update_data();
 116   3              }
 117   2      
 118   2              last_speed_scan_level = 1;
 119   2          } else {
 120   2              // 如果现在检测到低电平
 121   2              last_speed_scan_level = 0;
 122   2          }
 123   1      }
 124          
 125          void speed_scan(void)
 126          {
 127   1          volatile u32 cur_speed = 0;
 128   1          volatile u32 tmp = 0;
 129   1      
 130   1          if (cur_speed_scan_time >= SPEED_SCAN_UPDATE_TIME || flag_is_speed_scan_over_time) {
 131   2              /*
 132   2                  采集到的脉冲个数 / 一圈对应的脉冲个数 * 车轮一圈对应走过的距离（�
             -��位：mm），
 133   2                  计算得到 采集的脉冲个数对应走过的距离（单位：mm）
 134   2              */
 135   2              tmp = ((cur_speed_scan_pulse * SPEED_SCAN_MM_PER_TURN) * SPEED_SCAN_PULSE_COMPENSATION / SPEED_SCA
             -N_PULSE_PER_TURN);
 136   2      
 137   2              // printf("cur_speed_scan_pulse %lu\n", cur_speed_scan_pulse);
 138   2      
 139   2              if (flag_is_speed_scan_over_time) // 超时，采集到的脉冲个数对应一直是0km/h，认�
             -�时速是0
 140   2              {
 141   3                  // printf("scan over time\n");
 142   3                  if (cur_speed_scan_pulse != 0) {
 143   4                      // 如果采集的脉冲个数不为0
 144   4                      cur_speed = 1;
 145   4                  } else {
 146   4                      cur_speed = 0;
 147   4                  }
 148   3              } else // 未超时，计算采集到的脉冲个数对应走过的距离，再转换成以 km/h �
             -��单位
 149   2              {
 150   3                  /*
 151   3                      采集的脉冲个数对应走过的距离（单位：mm）/ 采集所用的时间（单�
             -�：ms） == 速度（单位：mm/ms）
 152   3                      1mm/ms == 1m/s
 153   3                      因为 1mm/ms * 1000 == 1m/ms
 154   3                           1m/ms  / 1000 == 1m/s
 155   3                           先乘以1000再除以1000，那么这个操作就可以去掉，直接化简为 1mm/
             -ms == 1m/s
 156   3      
 157   3                      1m/s == 3.6km/h，那么根据得到的 以 m/s 为单位的速度，先乘以36再除以1
             -0，得到以km/h为单位的速度值
 158   3      
 159   3                      cur_speed == 采集的脉冲个数对应走过的距离（单位：mm）/ 采集所用的�
             -��间（单位：ms）* 3.6
 160   3                      换成单片机可以计算的格式：
 161   3                      cur_speed == 采集的脉冲个数对应走过的距离（单位：mm） * 36 / 10 / 采�
             -�所用的时间（单位：ms）
 162   3                  */
 163   3                  cur_speed = tmp * 36 / 10 / cur_speed_scan_time;
 164   3              }
 165   2      
 166   2              // 防止时速为0时（有可能是推车，记录不到速度），记录不到里程
C51 COMPILER V9.60.7.0   SPEED_SCAN                                                        08/14/2026 16:22:47 PAGE 4   

 167   2              distance += tmp;
 168   2      
 169   2              // printf("cur distace 2 %lu\n", distance);
 170   2              // printf("cur distace %lu\n", distance);
 171   2              // printf("cur speed == %lu \n", cur_speed);
 172   2              // printf("cur_speed_scan_pulse == %lu\n", cur_speed_scan_pulse);
 173   2      
 174   2              cur_speed_scan_pulse = 0;
 175   2              cur_speed_scan_time = 0;
 176   2              flag_is_speed_scan_over_time = 0;
 177   2      
 178   2              if (cur_speed > 0) {
 179   3                  // 显示时加入了一阶低通滤波，这里给得到的速度加上补偿
 180   3                  cur_speed = (u32)cur_speed * 102 / 100;
 181   3              }
 182   2      
 183   2              // 限制要发送的时速:
 184   2              if (cur_speed > 199) {
 185   3                  cur_speed = 199;
 186   3              }
 187   2      
 188   2              instrument.speed = cur_speed;
 189   2          }
 190   1      }
 191          
 192          /**
 193           * @brief 递增AIP3368H显示速度刷新时间计数
 194           *
 195           */
 196          void aip3368h_display_speed_refresh_time_add(void)
 197          {
 198   1          if (!(UI_STATE_NORMAL == ui_manager.state ||
 199   1                UI_STATE_SETTING_DISTANCE_UNIT_TYPE == ui_manager.state)) {
 200   2              // 不在 正常界面 ，或者不在 设置 要显示的单位类型界面，直接返回
 201   2              aip3368h_display_speed_refresh_time_cnt = 0;
 202   2              return;
 203   2          }
 204   1      
 205   1          // 防止计数溢出
 206   1          if (aip3368h_display_speed_refresh_time_cnt < ((u8)-1)) {
 207   2              aip3368h_display_speed_refresh_time_cnt++;
 208   2          }
 209   1      }
 210          
 211          #if 1
 212          /**
 213           * @brief 根据单位类型（公制 或 英制）来显示时速
 214           *
 215           * @param speed 公制单位下的速度值
 216           */
 217          void aip3368h_display_speed_by_unit_type(u8 speed)
 218          {
 219   1          if (DISTANCE_UNIT_TYPE_METRIC == instrument.save_info.distance_unit_type) {
 220   2              // 显示时速
 221   2              aip3368h_display_speed(speed);
 222   2          } else if (DISTANCE_UNIT_TYPE_IMPERIAL == instrument.save_info.distance_unit_type) {
 223   2              // 1km/h == 0.621427mile/h
 224   2              // 显示时速
 225   2              aip3368h_display_speed((u32)speed * 621 / 1000);
 226   2          }
 227   1      }
 228          
C51 COMPILER V9.60.7.0   SPEED_SCAN                                                        08/14/2026 16:22:47 PAGE 5   

 229          void aip3368h_display_speed_handle(void)
 230          {
 231   1          static volatile u8 is_initialized = 0; // 是否初始化
 232   1          // static volatile u8 speed_of_lag = 0;   // 延迟显示的时速
 233   1          volatile u8 cur_speed; //
 234   1      
 235   1          /*
 236   1              一阶低通滤波器配置，滤波系数 α ，范围：0 ~ 10，推荐值 1 ~ 3
 237   1              α越小越平滑但响应慢,α越大响应快但平滑差
 238   1          */
 239   1      #define ALPHA 7 // 滤波系数 α
 240   1          static volatile u8 filtered_speed;
 241   1          u8 base_step;
 242   1          u8 speed_abs_diff;
 243   1      
 244   1          if (!(UI_STATE_NORMAL == ui_manager.state ||
 245   1                UI_STATE_SETTING_DISTANCE_UNIT_TYPE == ui_manager.state)) {
 246   2              // 不在 正常界面 ，或者不在 设置 要显示的单位类型界面，直接返回
 247   2              is_initialized = 0; //
 248   2              return;
 249   2          }
 250   1      
 251   1          if (0 == is_initialized) {
 252   2              is_initialized = 1;
 253   2      
 254   2              instrument.speed_of_lag = instrument.speed; // 初始化，直接获取当前最新的速度值
 255   2              filtered_speed = instrument.speed;
 256   2              aip3368h_display_speed_by_unit_type(instrument.speed_of_lag);
 257   2              speed_filter_init(instrument.speed);
 258   2      
 259   2              if (instrument.save_info.distance_unit_type == DISTANCE_UNIT_TYPE_METRIC) {
 260   3                  // 公制单位： km/h
 261   3                  aip3368h_display_mph_light(0);
 262   3                  aip3368h_display_kmh_light(1);
 263   3              } else {
 264   3                  // 英制单位： mile/h
 265   3                  aip3368h_display_kmh_light(0);
 266   3                  aip3368h_display_mph_light(1);
 267   3              }
 268   2          }
 269   1      
 270   1      #if 1
 271   1          if (aip3368h_display_speed_refresh_time_cnt >= AIP3368H_DISPLAY_SPEED_REFRESH_TIME) {
 272   2              aip3368h_display_speed_refresh_time_cnt = 0;
 273   2      
 274   2              // 如果当前显示的速度值和计算出来的速度值相差太大，需要进行快速逼�
             -�：
 275   2              if (instrument.speed_of_lag > instrument.speed) {
 276   3                  speed_abs_diff = instrument.speed_of_lag - instrument.speed;
 277   3                  if (speed_abs_diff >= 10) {
 278   4                      // 根据速度插值，调节步长
 279   4                      if (speed_abs_diff >= 50) {
 280   5                          base_step = 20;
 281   5                      } else if (speed_abs_diff >= 20) {
 282   5                          base_step = 10;
 283   5                      } else {
 284   5                          base_step = 5;
 285   5                      }
 286   4      
 287   4                      instrument.speed_of_lag -= base_step;
 288   4      #if USER_DEBUG_ENABLE
                              // printf("instrument.speed_of_lag == %u\n", (u16)instrument.speed_of_lag);
C51 COMPILER V9.60.7.0   SPEED_SCAN                                                        08/14/2026 16:22:47 PAGE 6   

              #endif
 291   4      
 292   4                      speed_filter_init(instrument.speed);
 293   4                      filtered_speed = instrument.speed;
 294   4                      aip3368h_display_speed_by_unit_type(instrument.speed_of_lag);
 295   4                      return;
 296   4                  }
 297   3              } else if (instrument.speed_of_lag < instrument.speed) {
 298   3                  speed_abs_diff = instrument.speed - instrument.speed_of_lag;
 299   3                  if (speed_abs_diff >= 10) {
 300   4                      // 根据速度插值，调节步长
 301   4                      if (speed_abs_diff >= 50) {
 302   5                          base_step = 20;
 303   5                      } else if (speed_abs_diff >= 20) {
 304   5                          base_step = 10;
 305   5                      } else {
 306   5                          base_step = 5;
 307   5                      }
 308   4      
 309   4                      instrument.speed_of_lag += base_step;
 310   4      #if USER_DEBUG_ENABLE
                              // printf("instrument.speed_of_lag == %u\n", (u16)instrument.speed_of_lag);
              #endif
 313   4      
 314   4                      speed_filter_init(instrument.speed);
 315   4                      filtered_speed = instrument.speed;
 316   4      
 317   4                      aip3368h_display_speed_by_unit_type(instrument.speed_of_lag);
 318   4                      return;
 319   4                  }
 320   3              }
 321   2      
 322   2              speed_filter_add(instrument.speed);
 323   2              cur_speed = speed_filter_get_speed();
 324   2              /*
 325   2                  如果当前速度值和过滤后的速度值都是10以下，需要注意
 326   2                  不能再经过低通滤波器，会导致最后计算出的时速变为0
 327   2              */
 328   2              if (cur_speed < 10) {
 329   3                  filtered_speed = cur_speed;
 330   3              } else {
 331   3                  // 套用一阶低通滤波器计算公式：
 332   3                  filtered_speed = ((u16)ALPHA * cur_speed + ((u16)10 - ALPHA) * filtered_speed) / 10;
 333   3              }
 334   2      
 335   2              cur_speed = filtered_speed;
 336   2      
 337   2              if (instrument.speed_of_lag > cur_speed) {
 338   3                  instrument.speed_of_lag--;
 339   3              } else if (instrument.speed_of_lag < cur_speed) {
 340   3                  instrument.speed_of_lag++;
 341   3              }
 342   2      
 343   2      #if USER_DEBUG_ENABLE
                      // printf("speed_of_lag == %u\n", (u16)speed_of_lag);
              #endif
 346   2              aip3368h_display_speed_by_unit_type(instrument.speed_of_lag);
 347   2          }
 348   1      #endif
 349   1      }
 350          #endif
 351          
C51 COMPILER V9.60.7.0   SPEED_SCAN                                                        08/14/2026 16:22:47 PAGE 7   

 352          #endif


MODULE INFORMATION:   STATIC OVERLAYABLE
   CODE SIZE        =   1149    ----
   CONSTANT SIZE    =   ----    ----
   XDATA SIZE       =     62      19
   PDATA SIZE       =   ----    ----
   DATA SIZE        =   ----    ----
   IDATA SIZE       =   ----    ----
   BIT SIZE         =      2    ----
END OF MODULE INFORMATION.


C51 COMPILATION COMPLETE.  0 WARNING(S),  0 ERROR(S)
